require 'spec_helper'

describe Doubletake do
  it 'has a version number' do
    expect(Doubletake::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
